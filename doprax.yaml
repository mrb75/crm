volumes:
  - name:  static
    mount: /app_data/static/
envs:
  - DB_PASS: @Amghjsrtipo_8430
    type: password
  - DB_NAME: crm_db
  - DB_HOST: postgres

services:
  - postgres:
      envs:
        - POSTGRES_ROOT_PASSWORD: random_1
          type: password
        - POSTGRES_DATABASE: crm_db
        - POSTGRES_USER: postgres
        - POSTGRES_PASSWORD: @Amghjsrtipo_8430
          type: password