volumes:
  - name:  static
    mount: /app_data/static/
envs:
  - DB_PASS: @Amghjsrtipo_8430
    type: password
  - DB_NAME: crm_db
  - DB_HOST: postgres

services:
  - postgresql:
      tag: 14.5
      volumes:
        - name: postgresql_vol
          mount: /var/lib/postgresql/
      envs:
        - POSTGRES_ROOT_PASSWORD: @Amghjsrtipo_8430
          type: password
        - POSTGRES_DATABASE: crm_db
        - POSTGRES_USER: postgres
        - POSTGRES_PASSWORD: @Amghjsrtipo_8430
          type: password